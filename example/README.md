# Example (Expo)

Minimal example app showcasing react-native-dotgrid.

- Ensure the example's Babel config includes `react-native-reanimated/plugin` as the last plugin.
- Run with a compatible Expo SDK (52+) in your environment.

```bash
npm install
npx expo start
```

